/// <reference types="vite/client" />

// Compile-time constant, populated from package.json by Vite's `define`
// (see vite.config.ts). Always available at runtime in both dev and prod.
declare const __APP_VERSION__: string;
